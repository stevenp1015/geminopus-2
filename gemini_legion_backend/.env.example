# Environment variables for Gemini Legion Backend
# Copy this file to .env in the SAME directory (gemini_legion_backend/) and fill in your actual values.
# OR, place your .env file in the project root (geminopus-2/) if you run the backend from there.

# Required for Google Generative AI
GOOGLE_API_KEY=YOUR_GOOGLE_API_KEY_HERE

# Optional: Override default model for minions
# DEFAULT_MODEL=gemini-1.5-pro-latest

# Optional: Database URL (if not using default SQLite)
# DATABASE_URL=postgresql+asyncpg://user:password@host:port/dbname

# Optional: Redis URL (if using Redis for caching or other purposes)
# REDIS_URL=redis://localhost:6379/0

# Debugging and Server
# DEBUG=True
# HOST=0.0.0.0
# PORT=8000
# RELOAD=True

# CORS Origins (space-separated if providing multiple via env var, or define in settings.py)
# CORS_ORIGINS="http://localhost:3000 http://localhost:5173 http://your-frontend-domain.com"
